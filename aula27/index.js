const userScore = 999;


// tipoVariavel nomeVariavel = (condiçao) ? 'se for verdadeiro' : 'se for falso';

const userLevel = (userScore >= 1000) ? 'usuario VIP' : 'usuario normal';

// MESMA COISA 

if (userScore >= 1000) {
    console.log('Usuario vip');
} else {
    console.log('Usuario normal');
}


