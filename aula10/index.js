const nome = 'Luiz'; //string
const num = 10; //number
let nomeAluno; //undefined
const sobreNomeAluno = null; //nao aponta para local na memória
const aprovado = false; //boolean -> sim ou não, 1 ou 0, true ou false.

console.log(`${nome} é uma variavel ${typeof nome}`)