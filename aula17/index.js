function soma(x, y) {
    const resultado = x + y;
    return resultado
};

//console.log(soma(2, 2));

const raiz = function (n) {
    resultado = Math.sqrt(n);
    return console.log(resultado);
};

//raiz(9);

const multi = n => n + 2
